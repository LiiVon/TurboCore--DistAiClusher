YAML配置管理类 ，持热更新、变更回调 ， 需下载YAML库

什么是YAML？ 
更好阅读的「数据序列化格式」，专门写配置文件

成员变量：
  1 递归互斥锁  mutable std::recursive_mutex m_mtx;
        mutable?  可以在const成员函数里 加锁
                  多线程下，一个线程热更新，一个线程读取配置，同时修改数据，所以加锁
        递归锁 ？  普通锁同一个线程加锁两次就会死锁，递归锁允许同一个线程多次加锁，不会死锁。
                   解析YAML是递归调用 某个方法，所以递归锁

 2 扁平化配置存储  std::unordered_map<std::string, std::any> m_configs
        扁平化 ？  YAML原生是嵌套结构，不是扁平的，即树形嵌套的，不是一维的，不方便。
                  类似下面这种
                  server:
                      port: 8888
                      workers:
                          - name: worker1
                            gpu_id: 0
                            model_path: /models/llama3-8b
                          - name: worker2
                            gpu_id: 1
                            model_path: /models/llama3-8b
                   扁平化存储将树形嵌套展开成一维的key-value map
                   server.port                 8888
                   server.workers.0.name       worker1
                   server.workers.0.gpu_id     0
                   server.workers.0.model_path  /models/llama3-8b

        map ？     unordered_map  O(1) 更快查询，读取配置
        any ？     配置值有多种类型，std::any可以类型擦除，更灵活

3 变更回调存储      std::unordered_map<std::string, std::vector<ConfigChangeCallback>> m_callbacks;
       什么用？     一个配值key对应 一系列变更回调，热更新key值变化，变量列表，通知key值变化了
       vector？     一个key 可以被多个业务模块注册回调

4 配置文件路径      std::string m_config_file_path

成员函数：
1 static Config& Instance();  全局唯一实例

2  LoadFromFile    从文件加载配置
       加锁？  加载过程修改了共享的m_configs，加锁保证多线程安全
       保存配置文件路径，后续热更新，用户不需要传入路径，直接用已经存的
       YAML 将文件解析成树形结构   再调用我们自己写的解析函数，将树形解析为扁平，并存入map

3  ParseYamlNode(const std::string& prefix, const YAML::Node& node)  递归扁平化
        YAML 有三种类型  
              Scalar：  port: 8080里的8080就是标量
              Map :     server: {port: 8080}里的server对应一个Map
              Sequence:  allowed_ips: [127.0.0.1, 192.168.1.1]就是Sequence
        3.1  Scalar：先判断root，然后转成string，尝试判断 int/double/bool/string/
        3.2  Map :  当前Map的键就是prefix,如果prefix空，key直接就是当前键名server
                    如果非空 就是 prefix.当前键名  然后递归调用
        3.3   Sequence:  一直是 key.next_key 的格式 递归

4  Reload    热更新重新加载配置，支持回滚
   保存旧配置 加载失败回滚  加载成功遍历回调map，对比变更，变更了触发回调 进行通知

5  IsConfigChanged  判断新旧配置的变化
      存在性变化，空值判断，类型判断

6 OnChange    配置key变更，调用该函数 

using ConfigChangeCallback = std::function<void(const std::string& key)>;
OnChange(const std::string& key, const ConfigChangeCallback& callback)
    eg：
// 用lambda当回调，完美符合ConfigChangeCallback类型
Config::Instance().OnChange("log.level", [](const std::string& changed_key) 
{
    // 配置变了，自动更新日志级别
    auto new_level = Config::Instance().Get<LogLevel>(changed_key, LogLevel::INFO);
    Logger::Instance().SetMinLevel(new_level);
    LOG_INFO("日志级别已经更新");
});
    加锁？ m_callbacks是所有线程共享的
    然后把回调函数存起来

7 template<typename T> 
 T Get(const std::string& key, const T& default_value = T()) const 泛型获取配置
    作用？   用户根据指定的 key，拿到对应类型的配置值
            配置只需要一次get
    读操作加锁？  一个线程读，一个线程同时加载配置文件或者热更新，会读一半旧被修改，所以加锁   
    判断key是否存在，不存在返回默认值，存在就ang_cast类型转换，返回结果



    
   
       





















