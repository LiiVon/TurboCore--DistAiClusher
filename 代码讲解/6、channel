Channel类：持有一个socket对应的fd，用户（上层使用者）告诉Channel：
  你要监听哪些事件（读/写） → event存在Channel里
  对应事件触发了，你要调用哪个回调函数 → callback存在Channel里
  Channel把这三样（fd+event+callback）绑定在一起，等着事件触发。
  即：Channel把Socket包装成Reactor能处理的事件通道，用户只需要设置回调、开关注事件，剩下的事Channel都帮你做了。

成员变量
1 EventLoop* m_ownerLoop  每个Channel只属于一个EventLoop
      用* 不用& ？   一个EventLoop有多个channel，指针表示指向，而不是&的独占
2  const SocketType m_fd; 
3   int m_event ， m_revents;	  关心的事件和实际发生的事件
    位掩码？ 
4    bool m_added;   是否已经添加到Poller中
5 读、写、错误、关闭的 回调函数
6 事件对应的位掩码 = 系统设定的常量 表示对应的读写事件
kNoneEvent = 0; kReadEvent = POLLIN; kWriteEvent = POLLOUT;

成员函数：
1、读、写、错误、关闭的回调函数，将用户传进来的 存在成员变量中
    std::function？  包装可调用对象，默认设置无参数、无返回值、更加通用
    
    有参怎么办？ 可以用lambda函数 将参数传入到 [] 进行捕获
    有返回值怎么办？  eg：
    int MyCallback(int a) {
    return a + 1;
    }

    int result; // 你自己的成员变量，存返回值
    channel->SetReadCallback([this, a]() {
    result = MyCallback(a); // 调用完把返回值存下来，Channel不用管
    });

2、这当中的位掩码操作如何理解？
    int 32位  能存储32个不同的开关，省空间，操作快
    都是1开 0关
    开事件： m_events |= kReadEvent;  读时间那位为1   或 之后 肯定是1
    关事件   m_events &= ~kReadEvent; 读事件那位变成0 再进行 与  肯定0
    检查事件开启了没  m_events & kReadEvent;  与1 & ，那一位1就1 ，0就0
    关闭所有事件    m_events = kNoneEvent;  直接全0
