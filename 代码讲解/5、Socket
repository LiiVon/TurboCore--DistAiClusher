Socket类 封装了原生相关API，封装后参数使用Address，更加方便
RAII的系统资源，不可拷贝，运行移动

SocketUtils工具类  封装了常用基础操作.工具类所有方法都是静态，不需要实例化

成员变量：
SocketType m_sockfd    原生socket句柄  
    OS内核会分配一个socket结构体，内核会返回一个对应的整数fd（文件描述符） 即当前的m_sockfd
    在用户态中操作,会把fd传给内核，内核通过fd找到对应的内核socket结果体，再执行对应的操作
    即：用户态操作fd，等价于操作内核里的socket。
    tips： 一个内核socket可以对应多个fd，但是一个fd不可能对应多个socket
           相当于是别名，socket有多个别名，但是一个别名只能有一个对应的原名

成员函数：只需注意跨平台即可
CreateNonBlocking  工厂方法




    
