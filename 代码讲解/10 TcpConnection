Tcp连接封装  所有IO事件在EventLoop驱动， TcpConnetcion 只负责处理回调

成员变量：
1 EventLoop* m_loop;               // 所属的EventLoop，
2 std::string m_name;              // 连接名
3 Socket m_socket;                 // 持有的Socket
4 std::unique_ptr <Channel> m_channel; // Socket对应的Channel，
5 Address m_peer_addr;             // 对端地址
6 std::any m_context;				// 用户自定义上下文
7 读、写缓冲区
8 	ConnectionState m_state;         // 连接状态，状态机流转
    连接的状态：Connecting  →  Connected  →  Disconnecting  →  Disconnected
9 各个事件回调

成员函数：
1 构造、析构
2 ConnecEstablished()  连接事件完成，注册事件，回调上层
3 ConnectDestroyed()   连接销毁，注销事件
      上面两个都是给TcpServer用的，
            accept完成连接，改变自身和持有的Channel的连接状态，把Channel注册到EventLoop，开始监听读事件
            TcpSever销毁连接时，依旧是改变自身和持有Channel的状态，并调用Channel的函数，停止关注所有事件
4 Send()    任意线程都可以使用，调用了所属loop的跨线程调用
5 Shutdown()  关闭，数据未发完则等待
6  ForceClose()  强制关闭，不等待未完成数据
 SendInLoop    ShutdownInLoop   ForceCloseInLoop
问：为什么拆解？
答：Reactor中，每个TcpConnection绑定一个EventLoop线程，所有对连接的修改，都得在Loop线程中执行
    Reactor的核心优势就是，一个Loop线程处理一个连接的所有事件，无锁，也就没有多余的开销
    所以一个是对外接口，一个是内部方法，用户任意线程调用send，都将使用到sendinloop；

7 获取相关信息的Get函数
8 设置回调函数

